server:
  port: 8088
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: http://localhost:8088/eureka
  server:
    wait-time-in-ms-when-sync-empty: 0
